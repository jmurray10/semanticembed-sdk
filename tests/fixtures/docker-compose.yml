version: "3.8"
services:
  frontend:
    image: nginx:alpine
    depends_on:
      - api
      - auth
  api:
    image: my-api:latest
    depends_on:
      - db
      - cache
  auth:
    image: my-auth:latest
    depends_on:
      - db
  db:
    image: postgres:16
  cache:
    image: redis:7
